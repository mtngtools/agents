description = "Create GitHub issue for prior WIP commit, rebase with issue number referenced."
prompt = """
This skill is human-only. Run it only because a human just asked for it by name. If you reached it any other way, stop and ask for explicit confirmation before running any step.

Read and follow the skill at `skills/repo/create-issue-to-rebase-wip/SKILL.md` in the mtngtools/agents repo (installed locally under `~/.agents/skills/create-issue-to-rebase-wip/SKILL.md`). Follow its instructions exactly.

Arguments, if any: {{args}}
"""
