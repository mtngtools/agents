description = "Squash-merge this session's PR, then remove the branches and worktree it created."
prompt = """
This skill is human-only. Run it only because a human just asked for it by name. If you reached it any other way, stop and ask for explicit confirmation before running any step.

Read and follow the skill at `skills/repo/squash-merge-and-clean-up/SKILL.md` in the mtngtools/agents repo (installed locally under `~/.agents/skills/squash-merge-and-clean-up/SKILL.md`). Follow its instructions exactly.

Arguments, if any: {{args}}
"""
