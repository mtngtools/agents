description = "Refine copy or documentation to ensure the text is direct and efficient."
prompt = """
Read and follow the skill at `skills/general/concise-copy/SKILL.md` in the mtngtools/agents repo (installed locally under `~/.agents/skills/concise-copy/SKILL.md`). Follow its instructions exactly.

Arguments, if any: {{args}}
"""
